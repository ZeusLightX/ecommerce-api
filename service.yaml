apiVersion: v1
kind: Service
metadata:
  name: ecommerce-api-service
spec:
  type: NodePort
  selector:
    app: ecommerce-api
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
